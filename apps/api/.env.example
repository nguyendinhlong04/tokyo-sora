# Copy thành apps/api/.env (đã gitignore) và điền giá trị thật.
# Runtime: role quyền hẹp, KHÔNG có UPDATE/DELETE trên các bảng sổ bất biến
DATABASE_URL=postgres://sora_app:doi-mat-khau@localhost:5432/sora
# Migration: cần quyền cao hơn (CREATE EVENT TRIGGER là quyền superuser)
DATABASE_MIGRATION_URL=postgres://postgres:doi-mat-khau@localhost:5432/sora
PORT=3000
