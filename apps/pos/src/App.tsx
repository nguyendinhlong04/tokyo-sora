export function App() {
  return (
    <main className="flex min-h-dvh flex-col items-center justify-center gap-2 bg-canvas font-sans text-ink-body">
      <h1 className="text-[length:var(--fs-t1)] font-semibold text-ink-hi">Sora POS</h1>
      <p className="text-[length:var(--fs-b2)]">
        Skeleton GĐ0 — vùng chạm: <span className="font-mono">var(--hit-target) = 52px</span>
      </p>
    </main>
  )
}
