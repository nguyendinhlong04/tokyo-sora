{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "BASE=\"$VERCEL_GIT_PREVIOUS_SHA\"; if [ -z \"$BASE\" ] || ! git cat-file -e \"$BASE^{commit}\" 2>/dev/null; then BASE=$(git rev-parse --verify -q HEAD^) || exit 1; fi; git diff --quiet \"$BASE\" HEAD -- ':/apps/staff' ':/packages' ':/pnpm-lock.yaml' ':/pnpm-workspace.yaml' ':/package.json'",
  "framework": "vite",
  "buildCommand": "pnpm --filter @sora/contracts build && pnpm --filter @sora/staff build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://api.tokyo-sora.vn/api/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Robots-Tag", "value": "noindex" },
        { "key": "X-Frame-Options", "value": "DENY" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
        {
          "key": "Content-Security-Policy",
          "value": "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'none'; form-action 'self'; base-uri 'self'"
        }
      ]
    }
  ]
}
